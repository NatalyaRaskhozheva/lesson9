const number = parseInt(prompt('Введіть кіькісь елементів масиву', '0'))

function half(number) {
	return size = number / 2
}

function creatArray(number, size) {
	arr = new Array(number).fill(1).fill(7, size)
	return arr
}

half(number)
creatArray(number, size)

document.write(`${arr}`)





